import HeroSection from "../sections/heroSection";

const Home = () => {
  return (
    <div className="bg-brand-blue flex flex-col h-full">
      <HeroSection />
    </div>
  );
};

export default Home;
